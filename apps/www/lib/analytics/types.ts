import {
  Topic,
  schemaIdByTopic,
  type TopicRecordPayloads,
} from "@vercel/data-streaming-interfaces"

export { Topic, schemaIdByTopic, type TopicRecordPayloads }

export enum UiShadcnSiteActivityAction {
  PageView = "PageView",
}
