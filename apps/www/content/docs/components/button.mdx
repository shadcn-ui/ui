---
title: Button
description: A versatile button component that supports various styles, states, and use cases.
featured: true
component: true
---

<ComponentPreview name="button-demo" description="A modern, accessible button component with multiple variants and states" />

## Features

- Multiple variants: primary, secondary, destructive, outline, ghost, and link
- Accessible by default with proper ARIA attributes
- Loading states with spinner
- Icon support with automatic spacing
- Link integration with Next.js and React Router
- Responsive design
- Customizable with Tailwind CSS

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add button
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ui/react-slot
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="button" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

### Basic Usage

```tsx
import { Button } from "@/components/ui/button"

export default function Page() {
  return (
    <div className="flex gap-4">
      <Button>Default Button</Button>
      <Button variant="outline">Outline Button</Button>
    </div>
  )
}
```

### With Icons

```tsx
import { Button } from "@/components/ui/button"
import { Mail, Send } from "lucide-react"

export default function Page() {
  return (
    <div className="flex gap-4">
      <Button>
        <Mail className="mr-2" />
        Send Email
      </Button>
      <Button variant="outline">
        Send
        <Send className="ml-2" />
      </Button>
    </div>
  )
}
```

### Loading State

```tsx
import { Button } from "@/components/ui/button"
import { useState } from "react"

export default function Page() {
  const [isLoading, setIsLoading] = useState(false)

  const handleClick = async () => {
    setIsLoading(true)
    await new Promise(resolve => setTimeout(resolve, 2000))
    setIsLoading(false)
  }

  return (
    <Button onClick={handleClick} disabled={isLoading}>
      {isLoading ? "Loading..." : "Click Me"}
    </Button>
  )
}
```

### As Link

You can use the `buttonVariants` helper to create a link that looks like a button.

```tsx
import { buttonVariants } from "@/components/ui/button"
import Link from "next/link"

export default function Page() {
  return (
    <div className="flex gap-4">
      <Link 
        href="/login" 
        className={buttonVariants({ variant: "outline" })}
      >
        Login
      </Link>
      
      <Button asChild>
        <Link href="/signup">Sign Up</Link>
      </Button>
    </div>
  )
}
```

## Examples

### Variants

<ComponentPreview name="button-demo" description="Primary button variant" />
<ComponentPreview name="button-secondary" description="Secondary button variant" />
<ComponentPreview name="button-destructive" description="Destructive button variant" />
<ComponentPreview name="button-outline" description="Outline button variant" />
<ComponentPreview name="button-ghost" description="Ghost button variant" />
<ComponentPreview name="button-link" description="Link button variant" />

### States

<ComponentPreview name="button-loading" description="Loading state" />
<ComponentPreview name="button-disabled" description="Disabled state" />

### With Icons

<ComponentPreview name="button-icon" description="Icon-only button" />
<ComponentPreview name="button-with-icon" description="Button with icon" />

### Sizes

<ComponentPreview name="button-sizes" description="Different button sizes" />

### As Child

<ComponentPreview name="button-as-child" description="Button as a wrapper component" />

## API Reference

### Button Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| variant | "default" \| "destructive" \| "outline" \| "secondary" \| "ghost" \| "link" | "default" | The visual style of the button |
| size | "default" \| "sm" \| "lg" \| "icon" | "default" | The size of the button |
| asChild | boolean | false | Whether to render as a child component |
| disabled | boolean | false | Whether the button is disabled |
| isLoading | boolean | false | Whether the button is in a loading state |

### buttonVariants Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| variant | "default" \| "destructive" \| "outline" \| "secondary" \| "ghost" \| "link" | "default" | The visual style of the button |
| size | "default" \| "sm" \| "lg" \| "icon" | "default" | The size of the button |

## Changelog

### 2024-10-16 Icon Styling

Added automatic icon styling with the following classes:
- `gap-2` for spacing between icon and text
- `[&_svg]:pointer-events-none` to prevent icon from capturing clicks
- `[&_svg]:size-4` for consistent icon sizing
- `[&_svg]:shrink-0` to prevent icon from shrinking

Add these classes to your `button.tsx` file:

```diff showLineNumbers title="button.tsx"
const buttonVariants = cva(
  "inline-flex ... gap-2 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0"
)
```
