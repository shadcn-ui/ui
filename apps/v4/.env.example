# -----------------------------------------------------------------------------
# Environment Variables (Local Development)
# -----------------------------------------------------------------------------
# The base URL where this app runs locally.
NEXT_PUBLIC_APP_URL=http://localhost:4000
# The base URL of the v0 site (used by Open in v0 button).
NEXT_PUBLIC_V0_URL=http://localhost:3000