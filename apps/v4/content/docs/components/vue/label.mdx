---
title: Label
description: Renders an accessible label associated with controls.
base: vue
component: true
---

<ComponentPreview framework="vue" name="label-demo" />

<Callout>
  For form fields, use the [Field](/docs/components/vue/field) component which
  includes built-in label, description, and error handling.
</Callout>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-vue/label
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="vue" name="label" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```vue showLineNumbers
<script setup lang="ts">
import { Label } from "@/components/ui/label"
</script>

<template>
  <Label for="email">Your email address</Label>
</template>
```

## Label in Field

For form fields, use the [Field](/docs/components/vue/field) component which
includes built-in `FieldLabel`, `FieldDescription`, and `FieldError` components.

```vue showLineNumbers
<script setup lang="ts">
import { Field, FieldLabel } from "@/components/ui/field"
import { Input } from "@/components/ui/input"
</script>

<template>
  <Field>
    <FieldLabel for="email">Your email address</FieldLabel>
    <Input id="email" />
  </Field>
</template>
```

<ComponentPreview
  framework="vue"
  name="field-demo"
  previewClassName="h-[44rem]"
/>

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview framework="vue" name="label-rtl" direction="rtl" />

## API Reference

See the [Radix UI Label](https://www.radix-ui.com/docs/primitives/components/label#api-reference) documentation for more information.
