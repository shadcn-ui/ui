---
title: Date Picker
description: A date picker component with range and presets.
base: ark
component: true
links:
  doc: https://ark-ui.com/docs/components/date-picker
  api: https://ark-ui.com/docs/components/date-picker#api-reference
---

<ComponentPreview styleName="ark-nova" name="date-picker-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add date-picker
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install @ark-ui/react @internationalized/date
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  name="date-picker"
  title="components/ui/date-picker.tsx"
  styleName="ark-nova"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

The date picker ships with convenience view components (`DatePickerDayView`, `DatePickerMonthView`, `DatePickerYearView`) that compose the full calendar views in a single line.

```tsx showLineNumbers
import {
  DatePicker,
  DatePickerContent,
  DatePickerControl,
  DatePickerDayView,
  DatePickerInput,
  DatePickerMonthView,
  DatePickerTrigger,
  DatePickerYearView,
} from "@/components/ui/date-picker"
```

```tsx showLineNumbers
<DatePicker>
  <DatePickerControl>
    <DatePickerInput />
    <DatePickerTrigger />
  </DatePickerControl>
  <DatePickerContent>
    <DatePickerDayView />
    <DatePickerMonthView />
    <DatePickerYearView />
  </DatePickerContent>
</DatePicker>
```

Each view component encapsulates the navigation controls, table headers, and cell rendering. For custom layouts, you can use the lower-level primitives (`DatePickerContext`, `DatePickerTable`, `DatePickerViewControl`, etc.) directly.

The Date Picker is built on [Ark UI's DatePicker](https://ark-ui.com/docs/components/date-picker), which uses [`@internationalized/date`](https://react-spectrum.adobe.com/internationalized/date/) for date handling.

## Examples

### Basic

A basic date picker component.

<ComponentPreview styleName="ark-nova" name="date-picker-basic" />

### Range Picker

Use `selectionMode="range"` with two inputs for date range selection.

<ComponentPreview styleName="ark-nova" name="date-picker-range" />

### Date of Birth

A date picker with month and year dropdowns using `DatePickerMonthSelect` and `DatePickerYearSelect`.

<ComponentPreview styleName="ark-nova" name="date-picker-dob" />

### Input

A date picker with an input field for typing dates directly.

<ComponentPreview styleName="ark-nova" name="date-picker-input" />

### Time Picker

A date picker with a time input field.

<ComponentPreview styleName="ark-nova" name="date-picker-time" />

### Natural Language Picker

Uses the `chrono-node` library to parse natural language dates via the `parse` prop.

<ComponentPreview styleName="ark-nova" name="date-picker-natural-language" />

## RTL

To enable RTL support in shadcn/ui, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview
  styleName="ark-nova"
  name="date-picker-rtl"
  direction="rtl"
/>

```tsx showLineNumbers
<DatePicker locale="ar-SA" dir="rtl">
  ...
</DatePicker>
```

## API Reference

See the [Ark UI DatePicker](https://ark-ui.com/docs/components/date-picker#api-reference) documentation for the full API reference.
