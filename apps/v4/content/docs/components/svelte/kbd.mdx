---
title: Kbd
description: Used to display textual user input from keyboard.
base: svelte
component: true
---

<ComponentPreview framework="svelte" name="kbd-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-svelte/kbd
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="svelte" name="kbd" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```svelte
<script lang="ts">
  import * as Kbd from "$lib/components/ui/kbd/index.js";
</script>
```

```svelte
<Kbd.Root>B</Kbd.Root>
```

## Composition

Use the following composition to build `Kbd.Root` and `Kbd.Group`:

```text
Kbd.Root
Kbd.Group
├── Kbd.Root
└── Kbd.Root
```

## Examples

### Group

Use the `Kbd.Group` component to group keyboard keys together.

<ComponentPreview framework="svelte" name="kbd-group" />

### Button

Use the `Kbd.Root` component inside a `Button` component to display a keyboard key inside a button.

<ComponentPreview framework="svelte" name="kbd-button" />

### Tooltip

You can use the `Kbd.Root` component inside a `Tooltip` component to display a tooltip with a keyboard key.

<ComponentPreview framework="svelte" name="kbd-tooltip" />

### Input Group

You can use the `Kbd.Root` component inside a `InputGroup.Addon` component to display a keyboard key inside an input group.

<ComponentPreview framework="svelte" name="kbd-input-group" />

### Primary

Use `variant="primary"` when placing a `Kbd` on a solid or brand-coloured surface so the key label remains legible against the background.

<ComponentPreview framework="svelte" name="kbd-primary" />

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview framework="svelte" name="kbd-rtl" direction="rtl" />

## API Reference

### Kbd.Root

Use the `Kbd.Root` component to display a keyboard key.

| Prop      | Type                     | Default     |
| --------- | ------------------------ | ----------- |
| `class`   | `string`                 | ``          |
| `variant` | `"default" \| "primary"` | `"default"` |

```svelte
<Kbd.Root>Ctrl</Kbd.Root>
```

### Kbd.Group

Use the `Kbd.Group` component to group `Kbd.Root` components together.

| Prop    | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | ``      |

```svelte
<Kbd.Group>
  <Kbd.Root>Ctrl</Kbd.Root>
  <Kbd.Root>B</Kbd.Root>
</Kbd.Group>
```
