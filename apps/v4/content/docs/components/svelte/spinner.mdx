---
title: Spinner
description: An indicator that can be used to show a loading state.
base: svelte
component: true
---

<ComponentPreview framework="svelte" name="spinner-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-svelte/spinner
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="svelte" name="spinner" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```svelte
<script lang="ts">
  import { Spinner } from "$lib/components/ui/spinner/index.js";
</script>
```

```svelte
<Spinner />
```

## Customization

You can replace the default spinner icon with any other icon by editing the `Spinner` component.

<ComponentPreview framework="svelte" name="spinner-custom" />

## Examples

### Colors

<ComponentPreview framework="svelte" name="spinner-colors" />

### Sizes

<ComponentPreview framework="svelte" name="spinner-sizes" />

### Size

Use the `size-*` utility class to change the size of the spinner.

<ComponentPreview framework="svelte" name="spinner-size" />

### Button

Add a spinner to a button to indicate a loading state. Place the `<Spinner />` before the label with `data-icon="inline-start"` for a start position, or after the label with `data-icon="inline-end"` for an end position.

<ComponentPreview framework="svelte" name="spinner-button" />

### Badge

Add a spinner to a badge to indicate a loading state. Place the `<Spinner />` before the label with `data-icon="inline-start"` for a start position, or after the label with `data-icon="inline-end"` for an end position.

<ComponentPreview framework="svelte" name="spinner-badge" />

### Input Group

<ComponentPreview framework="svelte" name="spinner-input-group" />

### Empty

<ComponentPreview framework="svelte" name="spinner-empty" />

## API

### Spinner

| Prop    | Type                                                 | Default     |
| ------- | ---------------------------------------------------- | ----------- |
| `color` | `"default" \| "primary" \| "onPrimary" \| "inherit"` | `"default"` |
| `size`  | `"xs" \| "sm" \| "md" \| "lg"`                       | `"sm"`      |

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview
  framework="svelte"
  name="spinner-rtl"
  direction="rtl"
/>
