---
title: Alert
description: Displays a callout for user attention.
base: ember
component: true
---

<ComponentPreview framework="ember" name="alert-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-ember/alert
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="ember" name="alert" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```gts showLineNumbers
import { Alert, AlertDescription, AlertTitle } from '@/ui/alert';
```

```hbs showLineNumbers
<Alert @variant="default">
  <Terminal />
  <AlertTitle>Heads up!</AlertTitle>
  <AlertDescription>
    You can add components and dependencies to your app using the cli.
  </AlertDescription>
</Alert>
```

## Examples

### Status Variants

Use `@variant="warning"`, `@variant="success"`, or `@variant="info"` for semantic status alerts.

<ComponentPreview
  framework="ember"
  name="alert-status"
  previewClassName="p-6"
/>

## API Reference

### Alert

The `Alert` component displays a callout for user attention.

| Prop       | Type                                                             | Default     |
| ---------- | ---------------------------------------------------------------- | ----------- |
| `@variant` | `"default" \| "destructive" \| "warning" \| "success" \| "info"` | `"default"` |
