---
title: Badge
description: Displays a badge or a component that looks like a badge.
base: ember
component: true
---

<ComponentPreview framework="ember" name="badge-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-ember/badge
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="ember" name="badge" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```gts showLineNumbers
import { Badge } from '@/ui/badge';
```

```hbs showLineNumbers
<Badge @variant="default">Badge</Badge>
<Badge @variant="outline">Outline</Badge>
<Badge @variant="secondary">Secondary</Badge>
<Badge @variant="destructive">Destructive</Badge>
```

## Examples

### Variants

Use the `@variant` argument to change the variant of the badge.

<ComponentPreview framework="ember" name="badge-variants" />

### Status Variants

<ComponentPreview framework="ember" name="badge-status" />

### With Icon

You can render an icon inside the badge. Use `data-icon="inline-start"` to render the icon on the left and `data-icon="inline-end"` to render the icon on the right.

<ComponentPreview framework="ember" name="badge-icon" />

### With Spinner

You can render a spinner inside the badge. Remember to add the `data-icon="inline-start"` or `data-icon="inline-end"` prop to the spinner.

<ComponentPreview framework="ember" name="badge-spinner" />

### Link

Use the `@asChild` argument to render a link as a badge.

<ComponentPreview framework="ember" name="badge-link" />

### Solid Status Variants

Use solid variants for maximum-attention states like critical counts or overdue flags.

<ComponentPreview framework="ember" name="badge-solid" />

### Custom Colors

You can customize the colors of a badge by adding custom classes such as `bg-green-50 dark:bg-green-800` to the `Badge` component.

<ComponentPreview framework="ember" name="badge-colors" />

## API Reference

### Badge

The `Badge` component displays a badge or a component that looks like a badge.

| Prop        | Type                                                                                                                                                                                     | Default     |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| `@variant`  | `"default" \| "secondary" \| "destructive" \| "outline" \| "ghost" \| "link" \| "warning" \| "success" \| "info" \| "success-solid" \| "warning-solid" \| "info-solid" \| "error-solid"` | `"default"` |
| `@class`    | `string`                                                                                                                                                                                 | -           |
