import { createIconLoader } from "./create-icon-loader"

// [FORCE-UI] Renders inlined Material Symbols (rounded) from the generated barrel.
export const IconMaterialSymbols = createIconLoader("materialSymbols")
