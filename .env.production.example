# Environment variables for production deployment
# Copy this file to .env.production and update with your actual values

# Database Configuration
DB_USER=ocean_erp
DB_PASSWORD=change_this_to_a_secure_password
DB_NAME=ocean_erp
DB_HOST=postgres
DB_PORT=5432

# Database URL (used by the app)
DATABASE_URL=postgresql://ocean_erp:change_this_to_a_secure_password@postgres:5432/ocean_erp

# NextAuth Configuration
NEXTAUTH_URL=http://103.168.135.110:4000
NEXTAUTH_SECRET=generate_a_random_secret_key_here_minimum_32_characters

# Application Configuration
NODE_ENV=production
PORT=4000

# Optional: Email Configuration (if needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com
