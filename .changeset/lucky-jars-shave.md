---
"shadcn": patch
---

Report why a components.json is invalid instead of a generic error. Zod validation issues are now included in the message, and the built-in registry override error is no longer swallowed.
