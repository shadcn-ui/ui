<!--
Lead Design Tokens v0.0.0-stub — hand-written target shape
This file will be generated by `design-tokens build` once the pipeline exists.
Do not edit directly; edit the rationale in DESIGN-DECISIONS.md and/or
the normalized tokens in /tokens/normalized/ and regenerate.
-->

# Lead Design System

**Version:** 0.0.0-stub
**Generated:** 2026-04-23 (hand-drafted placeholder)
**Source commit:** pending first generated build
**Rationale:** see `DESIGN-DECISIONS.md`

---

## Scope

This document is a **token inventory**. It describes the current state of the Lead design system's tokens, modes, and approved usages. It is regenerated on every token change. For the *why* behind any structure or rule, see `DESIGN-DECISIONS.md`.

## Modes

### Themes

| Mode  | Status      |
|-------|-------------|
| light | **active**  |
| dark  | planned, not implemented |

### Density

| Mode        | Status                        | Role                                     |
|-------------|-------------------------------|------------------------------------------|
| comfortable | stubbed (identical values)    | unfamiliar / infrequent / learning        |
| snug        | **active default**            | everyday, unmarked                       |
| compact     | stubbed (identical values)    | expert / high-frequency / data-dense     |

In v1, all three density modes resolve to identical values. Phase 2 populates real differences per `DESIGN-DECISIONS.md §5`.

### Runtime

- Theme: `[data-theme="light"]` on app root.
- Density: `[data-density="comfortable|snug|compact"]` on app root or any subtree.

---

## Primitives

### Colors

15 ramps × 10 steps = 150 color primitives. Abridged sample:

| Token                         | Hex       |
|-------------------------------|-----------|
| `primitives.colors.grey.50`   | `#F7F7F8` |
| `primitives.colors.grey.100`  | `#121215` |
| `primitives.colors.blue.600`  | `#...`    |
| `primitives.colors.red.600`   | `#DB1C08` |
| `primitives.colors.green.700` | `#149421` |

*Full ramp listing in `generated/token-reference.generated.md`.*

Ramps present: `red`, `orange`, `yellow`, `green`, `rainforest`, `ice`, `lima`, `blue`, `purple`, `magenta`, `berry`, `muted-blue`, `muted-red`, `grey`, `shift`.

### Numbers

| Token                  | Value |
|------------------------|-------|
| `primitives.numbers.2`   | 2   |
| `primitives.numbers.4`   | 4   |
| `primitives.numbers.8`   | 8   |
| `primitives.numbers.12`  | 12  |
| `primitives.numbers.16`  | 16  |
| `primitives.numbers.20`  | 20  |
| `primitives.numbers.24`  | 24  |
| `primitives.numbers.32`  | 32  |
| `primitives.numbers.48`  | 48  |
| `primitives.numbers.128` | 128 |

---

## Semantic — Foundation

Layout chrome. Surface, content, stroke, shift.

### Surface

| Token                                     | References                        | Value (light) |
|-------------------------------------------|-----------------------------------|---------------|
| `semantic.foundation.surface.0`           | `primitives.colors.grey.50`       | `#F7F7F8`     |
| `semantic.foundation.surface.1`           | `primitives.colors.grey.0`        | `#FFFFFF`     |

### Content

| Token                                     | References                        | Value (light) | Contrast on surface.1 |
|-------------------------------------------|-----------------------------------|---------------|-----------------------|
| `semantic.foundation.content.emphasis`    | `primitives.colors.grey.100`      | `#121215`     | 18.70:1 ✓ AAA         |
| `semantic.foundation.content.default`     | `primitives.colors.grey.500`      | `#494D5A`     | 8.43:1 ✓ AAA          |
| `semantic.foundation.content.muted`       | `primitives.colors.grey.700`      | `#7E8395`     | 3.77:1 ✗ small text   |
| `semantic.foundation.content.contrast`    | `primitives.colors.grey.0`        | `#FFFFFF`     | use on dark surfaces  |

### Stroke

| Token                                     | References                        | Value (light) |
|-------------------------------------------|-----------------------------------|---------------|
| `semantic.foundation.stroke.focus`        | *(pending normalization)*         | —             |
| `semantic.foundation.stroke.emphasis`     | *(pending normalization)*         | —             |
| `semantic.foundation.stroke.contrast`     | *(pending normalization)*         | —             |
| `semantic.foundation.stroke.default`      | *(pending normalization)*         | —             |
| `semantic.foundation.stroke.muted`        | *(pending normalization)*         | —             |

### Shift

| Token                                     | Value (light) |
|-------------------------------------------|---------------|
| `semantic.foundation.shift.default`       | `#4B5B9F`     |

*Note: the paper baseline collapses the historical shift duplicates to `shift.default`. If distinct shift steps are needed later, add them deliberately in `tokens/source/paper/tokens.paper.json`.*

---

## Semantic — Feedback

Status communication. Alert, success, warning, info.

| Token                                 | References                        | Value (light) | Contrast on surface.1 |
|---------------------------------------|-----------------------------------|---------------|-----------------------|
| `semantic.feedback.alert`             | `primitives.colors.red.700`       | `#F52512`     | 4.07:1 — *large text or bold only* |
| `semantic.feedback.success`           | `primitives.colors.green.700`     | `#149421`     | 3.97:1 — *large text or bold only* |
| `semantic.feedback.warning`           | *(yellow/amber primitive)*        | `#A87900`     | 3.89:1 — *large text or bold only* |
| `semantic.feedback.info`              | *(re-homed from `Generic`)*       | `#707EB2`     | 3.95:1 — *large text or bold only* |

**Usage rule:** feedback colors do NOT pass WCAG AA for small text on light surfaces. Permitted uses: icons, fills, pills, borders, and text at ≥18pt or ≥14pt bold. Enforced by `lint` rule L6 on `approvedPairs.json`.

---

## Semantic — Interactive

Action surfaces.

| Token                                 | References                        | Value (light) | Contrast on surface.1 |
|---------------------------------------|-----------------------------------|---------------|-----------------------|
| `semantic.interactive.primary`        | `primitives.colors.blue.600`      | `#3237FF`     | 6.72:1 ✓ AA all text  |

---

## Spacing

### Gap

| Token                   | Value |
|-------------------------|-------|
| `spacing.gap.min`       | 2     |
| `spacing.gap.2x-sm`     | 4     |
| `spacing.gap.x-sm`      | 8     |
| `spacing.gap.sm`        | 12    |
| `spacing.gap.med`       | 16    |
| `spacing.gap.lrg`       | 20    |
| `spacing.gap.x-lrg`     | 24    |
| `spacing.gap.2x-lrg`    | 32    |
| `spacing.gap.max`       | 48    |

### Padding

Identical values to Gap in v1. Separate collection per `DESIGN-DECISIONS.md §4.1`. CLI lint step warns on any drift that lacks a `justification` field.

| Token                   | Value |
|-------------------------|-------|
| `spacing.padding.min`   | 2     |
| `spacing.padding.2x-sm` | 4     |
| `spacing.padding.x-sm`  | 8     |
| `spacing.padding.sm`    | 12    |
| `spacing.padding.med`   | 16    |
| `spacing.padding.lrg`   | 20    |
| `spacing.padding.x-lrg` | 24    |
| `spacing.padding.2x-lrg`| 32    |
| `spacing.padding.max`   | 48    |

*(Density modes stub identical values; phase 2 populates differences.)*

---

## Radius

Frozen across density modes per `DESIGN-DECISIONS.md §5.4`.

| Token          | Value |
|----------------|-------|
| `radius.min`   | —     |
| `radius.x-sm`  | —     |
| `radius.sm`    | —     |
| `radius.med`   | —     |
| `radius.lrg`   | —     |
| `radius.x-lrg` | —     |
| `radius.max`   | —     |

*Values pending normalization.*

---

## Typography

### Roles

| Token                               | Family              | Weight   | Tracking | Case    |
|-------------------------------------|---------------------|----------|----------|---------|
| `typography.roles.body`             | Lead Sans Variable  | Regular  | 0        | Original|
| `typography.roles.body-emphasis`    | Lead Sans Variable  | Medium   | 0        | Original|
| `typography.roles.title`            | Lead Sans Variable  | Regular  | 0        | Original|
| `typography.roles.title-emphasis`   | Lead Sans Variable  | DemiBold | 0        | Original|
| `typography.roles.heading`          | Lead Sans Variable  | Regular  | 0        | Original|
| `typography.roles.display`          | Lead Sans Variable  | Regular  | varies   | Original|
| `typography.roles.caption`          | Lead Sans Variable  | DemiBold | 0        | Original|
| `typography.roles.code`             | Lead Sans Variable  | varies   | 0        | Upper   |

### Sizes

| Token                          | Value (snug) | Notes |
|--------------------------------|--------------|-------|
| `typography.sizes.code-xs`     | 10           | `nonText` (all-caps badge/tag) — exempt from text floor |
| `typography.sizes.code-md`     | 14           | |
| `typography.sizes.caption-sm`  | 12           | Meets text floor |
| `typography.sizes.caption-md`  | 12           | |
| `typography.sizes.body-sm`     | 14           | |
| `typography.sizes.body-md`     | 16           | |
| `typography.sizes.body-lg`     | 18           | |
| `typography.sizes.title-sm`    | 20           | |
| `typography.sizes.title-md`    | 22           | |
| `typography.sizes.title-lg`    | 25           | |
| `typography.sizes.heading-sm`  | 28           | |
| `typography.sizes.heading-md`  | 32           | |
| `typography.sizes.heading-lg`  | 36           | |
| `typography.sizes.display-sm`  | 40           | |
| `typography.sizes.display-md`  | 45           | |
| `typography.sizes.display-lg`  | 50           | |
| `typography.sizes.display-xl`  | 60           | |

Floor rules per `DESIGN-DECISIONS.md §5.3`:
- Text-classified sizes: minimum 12px (13px for `body-sm`).
- `nonText`-classified sizes (e.g., `code-xs`): exempt from text floor, validated under WCAG 1.4.11 (3:1) via approved pairs.
- Source-data floor violations must be listed in `/tokens/authored/sourceExceptions.json` with a resolution date (`[D36]`). Unexempted violations fail lint.

### Line-heights

| Token                            | Value |
|----------------------------------|-------|
| `typography.line-heights.tight`  | 1.0   |
| `typography.line-heights.snug`   | 1.25  |
| `typography.line-heights.normal` | 1.5   |

### Composition examples

| Intent                  | Role               | Size            | Line-height |
|-------------------------|--------------------|-----------------|-------------|
| Body copy (paragraph)   | `body`             | `body-md`       | `normal`    |
| Input value             | `body`             | `body-md`       | `tight`     |
| Card title              | `title-emphasis`   | `title-md`      | `snug`      |
| Standalone caption      | `caption`          | `caption-md`    | `tight`     |
| Page display            | `display`          | `display-lg`    | `snug`      |
| Code badge              | `code`             | `code-xs`       | `tight`     |

### Typography drifts and exceptions (from normalization report)

**Exempted via `/tokens/authored/sourceExceptions.json`** `[D36, D38]`:

No active source exceptions in the paper baseline. If a temporary source-data issue is intentionally tolerated, the exception must be added to `/tokens/authored/sourceExceptions.json` before normalize/lint can accept it.

**Warn-only drifts (no emission blockage, pending designer decision):**

- `title-md-emphasis`: missing `no-leading/` pair in source.
- `title-lg-emphasis`: doesn't exist.
- Code scale: missing S, L, XL variants (only XS and MD present).
- Cross-scale letter-spacing inconsistency: Title-Sm-Emphasis (−1%) vs. Title-Md-Emphasis (0%). Different roles, no emission conflict, warn only.

A paired-variant letter-spacing mismatch (`leading/X` vs `no-leading/X` of the same role disagreeing on tracking) would be hard-fail requiring an `L7-tracking-mismatch` exception — not currently present in this source.

CLI never synthesizes missing pairs or picks between conflicting variants without an explicit exception — see `DESIGN-DECISIONS.md §4.3.3`.

---

## Approved pairs

Authored in `/tokens/authored/approvedPairs.json`. Schema validated by co-located `approvedPairs.schema.json`. Per `DESIGN-DECISIONS.md §6.2`, each pair declares an explicit `textSize` (`normal | large | non-text`) and `targetLevel` (`AA | AAA`) that determine the lint threshold.

| fg                                      | bg                                  | textSize | targetLevel | usage                         |
|-----------------------------------------|-------------------------------------|----------|-------------|-------------------------------|
| `semantic.foundation.content.default`   | `semantic.foundation.surface.0`     | normal   | AA          | body text on default surface  |
| `semantic.foundation.content.default`   | `semantic.foundation.surface.1`     | normal   | AA          | body text on elevated surface |
| `semantic.foundation.content.emphasis`  | `semantic.foundation.surface.0`     | normal   | AAA         | emphasized headings and body  |
| `semantic.interactive.primary`          | `semantic.foundation.surface.1`     | normal   | AA          | links, primary CTAs           |
| `semantic.feedback.alert`               | `semantic.foundation.surface.1`     | non-text | AA          | alert icons, pills, borders   |
| `semantic.feedback.success`             | `semantic.foundation.surface.1`     | non-text | AA          | success icons, pills, borders |

*Note: `semantic.foundation.content.muted` (3.77:1 on white) does **not** qualify as an approved pair for any text usage — AA `normal` requires 4.5:1, and captions do not qualify as `large` (WCAG large = ≥18pt or ≥14pt bold). Use `content.default` for captions, or reserve `muted` for `non-text` use (decorative strokes, secondary icons).*

Source of truth: `/tokens/authored/approvedPairs.json`.

---

## Component tokens

### Non-conformant (v1 pass-through)

| Token                | Status                          | Phase 2 plan                          |
|----------------------|---------------------------------|---------------------------------------|
| `components.button.*`| Flat — one color per variant    | Anatomy (bg/fg/border) × states       |

Phase 2 will define full button anatomy. Current flat values flow through unchanged.

---

## Domain tokens

*Reserved. Populated in phase 2 or phase 3. Anticipated namespaces:*

- `domain.rail.*` — ACH, wire, check, internal, RTP, FedNow, crypto, intl wire, loan
- `domain.txn.status.*` — initiated, pending, posted, returned, disputed
- `domain.case.status.*` — pending, escalated, resolved
- `domain.entity.type.*` — consumer, business
- `domain.account.type.*` — FBO, virtual, originator

See `DESIGN-DECISIONS.md §10`.

---

## Changelog

See `/CHANGELOG.md`.

---

*This document is generated. To propose changes, edit the paper source in `/tokens/source/paper/`, run `design-tokens import --force`, `design-tokens normalize`, and regenerate with `design-tokens build`. To propose changes to rationale or rules, edit `DESIGN-DECISIONS.md`.*
