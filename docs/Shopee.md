# Shopee API — get_open_campaign_added_product

Summary of the `get_open_campaign_added_product` endpoint and parameters (extracted from Shopee.rtf).

## Endpoint URLs

- Global / SEA: https://partner.shopeemobile.com/api/v2/ams/get_open_campaign_added_product
- Chinese Mainland: https://openplatform.shopee.cn/api/v2/ams/get_open_campaign_added_product
- Brazil: https://openplatform.shopee.com.br/api/v2/ams/get_open_campaign_added_product
- Sandbox (SG): https://openplatform.sandbox.test-stable.shopee.sg/api/v2/ams/get_open_campaign_added_product
- Sandbox (CN): https://openplatform.sandbox.test-stable.shopee.cn/api/v2/ams/get_open_campaign_added_product

## Common (authentication) parameters

- `partner_id` (int) — Partner ID assigned on successful registration. Required.
- `timestamp` (timestamp) — Unix timestamp of request (seconds). Required. Expires in 5 minutes.
- `access_token` (string) — Token for API access (identifies permissions). Valid for multiple uses; example in docs: `c09222e3fc40ffb25fc947f738b1abf1`. Expires in ~4 hours.
- `shop_id` (int) — Shopee shop identifier. Required for most APIs.
- `sign` (string) — Signature generated by `partner_id`, API path, `timestamp`, `access_token`, `shop_id` and `partner_key` using HMAC-SHA256. See Shopee docs: https://open.shopee.com/documents?module=87&type=2&id=58&version=2

## Request Parameters

- `page_size` (int32) — Maximum entries per page. Range 1–100. Sample: `20`.
- `cursor` (string) — Cursor for paging. Default empty. Pass the `cursor` value from the last response to get the next page.
- `sort_by` (string) — Field to sort by. Available values depend on API (e.g., `commission_rate`, `-commission_rate` for descending).
- `search_type` (string) — `ITEM_NAME` or `ITEM_ID`.
- `search_content` (string) — Content to search by (item name or comma-separated item IDs). If using `ITEM_ID`, split by comma and at most 50 items.

Notes:
- `page_size` controls the number of results per call; use `cursor` and `has_more` to paginate.

## Response Parameters (high level)

- `error` (string) — Empty if no error; otherwise an error code (e.g., `error_business`).
- `message` (string) — Error message or details (e.g., "Please agree to the AMS T&C in Seller Center - Affiliate Marketing Solutions first before using Open API.").
- `request_id` (string) — Identifier for API request for error tracking.
- `response` (object) — Payload containing the data; may include `item_list`, `total_count`, `cursor`, `has_more`.

### `response.item_list[]` (per item)

- `item_id` (int64)
- `item_name` (string)
- `campaign_id` (int64)
- `campaign_status` (string) — e.g., `Upcoming`, `Ongoing`, `Terminating`.
- `commission_rate` (float) — Commission rate; `1.11` means `1.11%`. Supports two decimal places.
- `period_start_time` (timestamp)
- `period_end_time` (timestamp) — If `32503651199` (2999-12-31 23:59:59) it means no limit.
- `pending_terminated_time` (timestamp)
- `commission_protection_list` (object[]) — Commission protection details (each entry may include `commission_rate`, `protection_period_end_time`, etc.)

### Pagination fields

- `total_count` (int32) — Total number of items matching the condition.
- `cursor` (string) — Use in next request to retrieve next page.
- `has_more` (boolean) — `true` if more pages are available.

## Examples & Notes

- Signature (`sign`) generation: HMAC-SHA256 over the canonical string derived from `partner_id`, API path, `timestamp`, `access_token`, `shop_id`, and `partner_key`. Refer to Shopee docs: https://open.shopee.com/documents?module=87&type=2&id=58&version=2
- Timestamps are Unix seconds. `timestamp` (request) must be within 5 minutes of server time.
- `access_token` expiry: generally a few hours — obtain via OAuth/token flow as documented by Shopee.

---

File generated from `docs/Shopee.rtf` by extracting readable text. If you want a fuller, formatted conversion (preserving tables and styling), I can run a proper RTF→Markdown conversion and include table markup for each parameter set.

Next steps you may want:

- Preview `docs/Shopee.md` in your editor or with a Markdown viewer.
- If you maintain a docs site, add this file to the site build (I can help wire it into your docs pipeline).
