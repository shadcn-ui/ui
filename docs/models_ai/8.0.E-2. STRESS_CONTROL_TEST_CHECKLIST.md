# RUNTIME STRESS & CONTROL TEST CHECKLIST

- Simulate peak traffic load while control_state = SUPERVISED_AUTONOMY
- Force partial service outage during active transaction processing
- Block API requests when control_state = MANUAL_ONLY
- Trigger kill-switch activation during in-flight operations
- Verify all write operations are rejected after kill-switch activation
- Attempt unauthorized control_state override via API
- Attempt recovery without multi-party authorization
- Restart services while kill-switch remains active
- Validate audit logs are written under failure conditions
- Confirm system remains fail-closed under degraded dependencies
